# Stargazers Grabber for github

## Install dependencies
    npm i

## Setup env variables
Rename `.env.example` to `.env`
and set `CLIENT_ID` and  `CLIENT_SECRET` from your github account.

## Usage
See usage.js for example.
